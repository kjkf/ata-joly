;(function () {
  /*  const mql1200 = window.matchMedia("(max-width: 1200px)");
    const mql1023 = window.matchMedia("(max-width: 1023px)");
    const mql992 = window.matchMedia("(max-width: 992px)");
    const mql576 = window.matchMedia("(max-width: 576px)");

    const langMenu = document.querySelector('.lang-menu');
    const socialMenu = document.querySelector('.social-menu');

    function handlerForMediaQueries(x) {
        if (mql1023.matches) { // If media query matches
            const headerMobileContainer = document.querySelector('.header-mob .container');
            headerMobileContainer.appendChild(langMenu);
            headerMobileContainer.appendChild(socialMenu);
        } else {
            const headerMobileContainer = document.querySelector('.header .container');
            const search = headerMobileContainer.querySelector('.search');
            headerMobileContainer.insertBefore(socialMenu, search);
            const headerNav = headerMobileContainer.querySelector('.header__nav');
            headerMobileContainer.insertBefore(langMenu, headerNav);
        }
    }

    handlerForMediaQueries();
    mql1023.addEventListener("change", () => {
        handlerForMediaQueries();
    });
*/
})();